// ─────────────────────────────────────────────
// LEGEND STYLE shared by the SVG legends of the charts (charts/lib/ChartLegend.tsx) and of the 2D views
// (views/svg/Legend.tsx), so both look the same. Label colour and size: legend.module.css `.text`.
// ─────────────────────────────────────────────

/** Label font size in px (legend.module.css `.text`). */
export const LEGEND_FONT_SIZE = 12;
/** Width of a swatch: length of a line swatch, width of a band. */
export const LEGEND_SWATCH_W = 16;
/** Side of a square swatch (areas, bars), centred in the swatch width. */
export const LEGEND_SQUARE = 12;
/** Gap between swatch and label. */
export const LEGEND_SWATCH_GAP = 6;
/** Gap between two items in a row. */
export const LEGEND_ITEM_GAP = 16;
export const LEGEND_ROW_HEIGHT = 18;
