// ─────────────────────────────────────────────
// SURROUNDING BUILDINGS IN 3D (placeholder)
// Owned by the buildings feature (docs/ARCHITECTURE.md, "Umgebung", file ownership): imported and manual
// buildings as extruded prisms (removed ones hidden, edited ones marked), merged geometry (≤ 150 buildings,
// phones), consistent with Surroundings.tsx. Rendered by SceneContent in world coordinates (outside the
// facade-rotated group); the buildings feature adds its props (and the shadow-camera points) there.
// ─────────────────────────────────────────────

/** Surrounding buildings in the 3D scene (inside <Canvas>). Placeholder: renders nothing. */
export function Buildings3D() {
  return null;
}
