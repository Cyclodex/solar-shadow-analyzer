// ─────────────────────────────────────────────
// LASER-SCAN SETTINGS (placeholder)
// Owned by the laser-scan feature (docs/ARCHITECTURE.md, "Umgebung", file ownership): toggle «Laserscan-
// Umgebung (swisstopo)», status (loading with MB, data year, error with retry, «nur in der Schweiz und
// Liechtenstein verfügbar»), «Bäume berücksichtigen» (trees opaque all year), radius. Reads
// config.horizon.surfaceModel and dataStore.surface itself; HorizonSection renders it without props.
// ─────────────────────────────────────────────

/** Laser-scan horizon settings and status in the «Horizont & Umgebung» section. Placeholder: renders nothing. */
export function SurfaceModelControls() {
  return null;
}
